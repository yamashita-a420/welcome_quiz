<div class="common-container">
  <div class="flex flex-col title-font font-medium items-center text-gray-900 mb-4 md:mb-0">
    <p class="font-medium text-3xl text-gray-900 m-4"><%= t('.welcome_message') %></p>
    <p class="font-medium text-l text-gray-900 m-2">ささやかですが クイズを用意しましたので</p>
    <p class="font-medium text-l text-gray-900 m-2">お楽しみください</p>
    <%= form_with model: @first_question, url: first_question_path(@first_question), method: :get, local:true do |f| %>
      <%= f.hidden_field :second_question, :value => @second_question.id %>
      <%= f.hidden_field :third_question, :value => @third_question.id %>
      <%= f.submit t('.button'), class: 'choice-btn' %>
    <% end %>
  </div>
</div>
