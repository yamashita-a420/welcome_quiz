<div class="common-container">
  <div class="flex flex-col title-font font-medium items-center text-gray-900 mb-4 md:mb-0">
    <h1 class="text-5xl font-app my-10"><%= t('helpers.quiz') %></h1>
    <p class="font-medium text-3xl text-gray-900 m-4"><%= t('.welcome_message') %></p>
    <p class="font-medium text-l text-gray-900 m-2">ささやかですが</p>
    <p class="font-medium text-l text-gray-900 m-2">クイズをご用意しました</p>
    <p class="font-medium text-l text-gray-900 mt-2 mb-6">お楽しみ頂けたら幸いです</p>
    <%= form_with url: user_first_question_path(@first_question, user_id: @first_question.user_id), method: :get, local:true do |f| %>
      <%= f.hidden_field :second_question, :value => @second_question.id %>
      <%= f.hidden_field :third_question, :value => @third_question.id %>
      <%= f.submit t('.button'), class: 'flex justify-center text-center top-btn transition duration-500 ease select-none hover:bg-yellow-600 focus:outline-none focus:shadow-outline' %>
    <% end %>
  </div>
</div>
