<div class="hero min-h-screen bg-base-200">
  <div class="hero-content text-center">
    <div class="max-w-md">
      <h1 class="text-5xl font-app"><%= t('.title') %></h1>
      <p class="py-6"><%= t('.welcome_message') %></p>
      <div class="flex flex-col w-full border-opacity-50">
        <button class="grid h-20 btn btn-primary place-items-center"><%= link_to t('.try'), new_question_path %></button>
        <div class="divider">OR</div>
        <button class="grid h-20 btn btn-primary place-items-center"><%= link_to t('defaults.login'), login_path %></button>
      </div>
    </div>
  </div>
</div>
